In case of using a modern MVC supported UI technology like AngularJS, returning regular Spring MVC Model object may 
not be feasible to work with. 
Most of the modern JS technologies work with JSON responses. This example shows how to convert Model returned from controller to JSOn automatically.

Hit the initial URL to get the page and then click 'Get Json Response' button
